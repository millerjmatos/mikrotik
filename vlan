# Criando a VLAN no mk1:

# O procedimento é o mesmo para cada VLAN..

    Interfaces > VLAN > Add

        New Interface
        
            General

                Name: vlan100-eth3

                VLAN ID: 100

# Interface de saída para o outro mk (TAGGED):
                
                Interface: eth3-mk2

# A mesma interface física de saída pode ser usada para muitas VLANs ao mesmo tempo.

# Criar a VLAN também no mk2, com mesmo VLAN ID, e atentar para a interface física de saída (TAGGED)!
# Ex: eth1-mk1

# Definindo o IP de cada interface VLAN no mk1:

    IP > Addresses

        Addresses List > Add

            New Address

                Address: 10.10.10.1/24

                Interface: vlan100-eth3

                    IP > Addresses

# Definindo os servidores dhcp para cada VLAN:

    IP > DHCP SERVER > DHCP Setup

# Não esquecer do NAT!

# No mk2:

# Criar a bridge:

    Bridge > Bridge > Add

        General

            Name: bridge-vlan100

# Adicionar a interface UNTAGGED e VLAN na mesma bridge:

    Bridge > Ports > Add

        New Bridge Port

            Interface: eth2-vlan100

            Bridge: bridge-vlan100

    Bridge > Ports > Add

        New Bridge Port

            Interface: vlan100-eth1

            Bridge: bridge-vlan100

# Para outras VLANs a mesma configuração:

    Bridge > Ports > Add

        New Bridge Port

            Interface: eth5-vlan250

            Bridge: bridge-vlan250

    Bridge > Ports > Add

        New Bridge Port

            Interface: vlan250-eth1

            Bridge: bridge-vlan250

# Nesse exemplo, o esquema das interfaces no mk2 (SW) ficou assim:

eth1-mk1-tagged-interface
    vlan100-eth1
    vlan250-eth1
eth2-vlan100-untagged-interface
eth5-vlan250-untagged-interface
